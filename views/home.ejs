<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
  </head>
  <body>
    <h1>Hello World! Here is <%= user.firstname || "the home page" %>.</h1>
    <% if (user) { %>
    <p><a href="/logout">Log out</a></p>
    <% } %> <% if (!user) { %>
    <p><a href="/login">Login</a></p>
    <p><a href="/signup">Sign-Up</a></p>
    <% } %>
  </body>
</html>
